Install the library:

$ mvn install:install-file -Dfile=./AlexLib.jar -DgroupId=alex -DartifactId=AlexLib -Dversion=1.0


Install the dependency in pom.xml.

<dependency>
    <groupId>com.roufid.tutorials</groupId>
    <artifactId>example-app</artifactId>
    <version>1.0</version>
</dependency>


